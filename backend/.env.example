# MongoDB Configuration
# Required: MongoDB connection string
# Note: If MongoDB has authentication enabled, use this format:
# MONGO_URL=mongodb://root:password@localhost:27017/sales_analytics?authSource=admin
# For Docker: mongodb://root:password@mongodb:27017/sales_analytics?authSource=admin
MONGO_URL=mongodb://localhost:27017

# Database Name
# Required: Name of the MongoDB database
DB_NAME=sales_analytics

# Google OAuth Configuration
# Required: Google OAuth 2.0 Client ID from Google Cloud Console
# Get this from: https://console.cloud.google.com/apis/credentials
GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Google OAuth Redirect URI
# Optional: Defaults to http://localhost:3000
# Must match one of the authorized redirect URIs in Google Cloud Console
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3000

# CORS Configuration
# Optional: Comma-separated list of allowed origins (default: *)
# Example: http://localhost:3000,https://yourdomain.com
CORS_ORIGINS=*

# Google Sheets API Credentials
# Optional: JSON string of Google Service Account credentials for Google Sheets integration
# This is used for automated Google Sheets data uploads
# Format: {"type": "service_account", "project_id": "...", ...}
# Leave empty if not using Google Sheets integration
GOOGLE_SHEETS_CREDENTIALS=